{
	"name": "vscode-gitops-tools-webview-ui",
	"scripts": {
		"install": "npm --prefix ./configureGitOps install && npm --prefix ./createFromTemplate install",
		"build": "npm --prefix ./configureGitOps run build && npm --prefix ./createFromTemplate run build"
	}
}